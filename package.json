{
  "private": true,
  "name": "cloudflare-scriptrewriter",
  "version": "1.0.0",
  "description": "Rewrite HTML script tags to <template> tags",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Seppe Gadeyne",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
